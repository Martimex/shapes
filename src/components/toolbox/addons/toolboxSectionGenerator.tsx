// Temporarily not used
export default class ToolboxSection {

    title: string;
    elements: object[];

    constructor(title: string, elements: object[]) {
        this.title = title;
        this.elements = elements;
    }
}

/*
    Title = Section title
    Elements = how many buttons a given section includes

*/